就是把一个likedlist反过来，
一种方法是用一个栈，一次从头进去
再一次pop出来，顺序就反了，这个做法费空间
下面的方法比较简单，就是一前一后的指针，head一步一步往下走。


public class Solution {
    /**
     * @param head: The head of linked list.
     * @return: The new head of reversed linked list.
     */
    public ListNode reverse(ListNode head) {
        ListNode previous = null; 先定义为null， 
        ListNode nextNode = null;
    while (head != null) {  
       nextNode = head.next;  
        head.next = previous;  
        previous = head;  
        head = nextNode;  
    }  
          
    return previous;  
      
    }
}

