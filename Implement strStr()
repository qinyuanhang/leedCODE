
public class Solution {
    public int strStr(String haystack, String needle) {
    
    这里null和“”是有区别的
    有null就只能返回-1
    要是目标句子是“”就要返回0
        if( haystack == null || needle == null) return -1;
        
        int sL = haystack.length();
        int tL = needle.length();

        
        if(tL > sL ) return -1;
        
        if(tL == 0 && sL == 0) return 0;
       
        
        for(int p = 0; p < sL; p++){
            if(p + tL <= sL && haystack.substring(p,p+tL).equals(needle)) return p;
            用这个substring把p到p+目标句的长度框出来，要是equal的话就返回p就是当前的位置
        }
        
        
        
        return -1;
        
    }
}
