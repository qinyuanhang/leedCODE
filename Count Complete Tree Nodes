
计算一颗棵完全二叉树的所有节点，用的是递归

1，分别找出左边最大深度和右边最大深度
    要是相等，那就直接返回2^（深度）-1
    要是不相等，分别递归调用左边子树和右边子树，结果加1


public class Solution {
    public int countNodes(TreeNode root) {
        if(root == null) return 0;
        int lH = 0, rH = 0;
        TreeNode l = root, r = root;
        
        while(l.left != null){
            l = l.left;
            lH++;
        }
        while(r.right != null){
            r = r.right;
            rH++;
        }
        
        if(lH == rH){
            return (int) Math.pow(2, lH + 1) - 1;
        }
        
        return 1 + countNodes(root.left) + countNodes(root.right);  调用左右子树，结果加1
        
    }
}
