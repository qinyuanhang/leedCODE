就是用一个链表，再用inorder存起来，最后从头往后取就行了；

public class Solution {
    Queue<Integer> res = new LinkedList<Integer>();
    public int kthSmallest(TreeNode root, int k) {
        
        help(root);
        int rest = 0;
        for(int i = 0; i < k; i++){
            rest = res.poll();
        }
        return rest;
    }
     private void help(TreeNode root){
         if(root == null) return;
         help(root.left);
         res.offer(root.val);
         
         help(root.right);
     }
    
}
