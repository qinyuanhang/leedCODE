/**
Given two binary trees, write a function to check if they are equal or not.

Two binary trees are considered equal if they are structurally identical and the nodes have the same value.
*/

/**
 * Definition for binary tree
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode(int x) { val = x; }
 * }
 */
 
 /**
 简单的递归函数，其实我不知道什么叫递归，我的理解就是在函数中自己调用自己，return false是返回上一个点而不是结束整个
 程序，他会根据你的条件检查，要是不符合你的条件，就会返回上一个点，就像走迷宫， return false是回到上个岔路口
 
 */
 
public class Solution {

这里所有的条件都要考虑到，什么条件是返回上一个点，什么情况他们是相等的，
比如说他们都是null 和他们的值相等这两种情况都可以认为是相同的数的节点，所以返回的是true

    public boolean isSameTree(TreeNode p, TreeNode q) {
        if(p==null && q==null) return true;
        if(q!=null && p!=null){
            if(p.val==q.val){
                return isSameTree(p.left,q.left) && isSameTree(q.right,p.right);
            }else{
                return false;
            }
        }
        return false;
    }
}
